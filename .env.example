NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=sns-post-images

# Comma-separated handles allowed to view /admin. Leave unset to disable the
# page for everyone (it renders a not-found message instead of stats).
NEXT_PUBLIC_ADMIN_HANDLES=
